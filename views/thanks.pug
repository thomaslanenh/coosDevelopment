extends layout

block content
    section(style="flex: 1 0 auto; padding: 20px;")
        div(class='columns is-centered is-mobile')
            div(class="column")
                p(class='has-text-centered') Thank you for your submission.